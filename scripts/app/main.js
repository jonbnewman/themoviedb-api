define([
  "jquery", "postal", "knockout", "app/TheMovieDB" ],
  function( $, postal, ko, TheMovieDB ) {

    return function( opt ) {
      var main = this;
      var options = opt || {};

    };
  }
);